<div class="container">
  <h1 class="text-center">Profile</h1>
  <div class="row justify-content-center">
    <% if current_user.role == 'client'%>
      <div class="mb-15 " style="width: 70%;">
        <p>Name: <%= @user.username%></p>
        <p>Email: <%= current_user.email%></p>
        <p>Rating: <%=Client.find_by(user_id: current_user.id).score%></p>
        <p>Balance: <%=Client.find_by(user_id: current_user.id).balance%></p>
        <p>Reviews: <%=Client.find_by(user_id: current_user.id).score%></p>
        <div class="text-left">
          <%= link_to "Completed tasks", client_tasks_path(client_id: current_user.clients.first, tasks: Task.all.where(is_completed: true)), class:"btn btn-outline-info mx-auto" %>
          <%= link_to "Current tasks", client_tasks_path(client_id: current_user.clients.first, tasks: Task.all.where(is_assigned: false)), class:"btn btn-outline-info mx-auto" %>
          <%= link_to "Settings", edit_user_password_path, class:"btn btn-info mx-auto" %>
        </div>
      </div>
    <% elsif current_user.role == 'tasker' %>
      <div class="card mb-15 " style="width: 70%;">
      <p>Name: <%= user.username%></p>
      <p>Email: <%=  current_user.email %></p>
        <p>Rating: <%=Tasker.find_by(user_id: current_user.id).score%></p>
        <p>Balance: <%=Tasker.find_by(user_id: current_user.id).balance%></p>
        <p>Reviews: <%=Tasker.find_by(user_id: current_user.id).score%></p>
        <div class="text-left"> 
          <%= link_to "Completed tasks",  user_path, class:"btn btn-outline-info mx-auto" %>
          <%= link_to "Current tasks", user_path, class:"btn btn-outline-info mx-auto" %>
          <%= link_to "Settings", edit_user_password_path, class:"btn btn-info mx-auto" %>
        </div>
      </div>
    <% end %>
  </div>
</div>